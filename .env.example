# Optional. If set, the in-app Scent Guide uses Claude to answer.
# When unset, the API falls back to a deterministic on-house response.
ANTHROPIC_API_KEY=

# Token used to enter the /admin panel. Defaults to "etienne-admin" in dev.
ADMIN_TOKEN=etienne-admin
